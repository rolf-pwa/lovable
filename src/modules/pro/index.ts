/**
 * Public API of the `pro` module (Professional Spoke Alliance portal).
 *
 * Nothing outside this module consumes it today; the barrel exists so future
 * cross-module use goes through a single documented surface.
 */
export {};
