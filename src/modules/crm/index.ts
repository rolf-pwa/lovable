/**
 * Public API of the `crm` module.
 *
 * Other modules may only import from `@/modules/crm` — never from files
 * inside it. Deep cross-module imports are blocked by ESLint.
 */
export { StatementUpload } from "./components/StatementUpload";
