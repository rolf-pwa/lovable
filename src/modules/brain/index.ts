/**
 * Public API of the `brain` module (Second Brain / knowledge layer).
 *
 * Nothing outside this module consumes it today; the barrel exists so future
 * cross-module use (e.g. the CRM assistant surfacing a "captured to brain"
 * action) goes through a single documented surface.
 */
export {};
